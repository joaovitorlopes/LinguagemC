#include <stdio.h>
//recebe parametro array e o imprima, nao use indices para percorrer o array, apenas aritmetica de ponteiros
void imprime(int a[], int size){
  for(int i = 0; i < size; i++){
    printf("%d\n", *(a+i));
  }
}

int main() {
 int a[5] = {5,4,3,2,1};
 imprime(a,5);
  return 0;
}
