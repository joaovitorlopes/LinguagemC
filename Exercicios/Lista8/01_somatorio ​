#include <stdio.h>

int somatorio(int n){
if (n == 0) {
  return 0;
}else if(n == 1){
  return 1;
}else{
  return n + somatorio(n-1);
}
}

int main(){

int num;
scanf("%d",&num);
printf("%d\n",somatorio(num));
  return 0;
}
