#include <stdio.h>

int exp(int k, int n){
if (n == 1) {
  return k;
}else if(n == 0){
  return 1;
}else{
  return k * exp(k , n -1);
}
}

int main(){

int k,n;
scanf("%d",&k);
scanf("%d",&n);
printf("%d\n",exp(k,n));
  return 0;
}
