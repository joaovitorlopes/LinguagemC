#include <stdio.h>
#include <stdlib.h>
int main(){
int n,i,j, vetor[10];
scanf("%d",&n);
vetor[0] = n;
for (i=1; i<10; i++){
    scanf("%d",&vetor[i]);
    for (j=0; j<i; j++){
        if (vetor[j] == vetor[i]){
            printf("%d Já foi digitado antes!\n",vetor[i]);
	    i = i - 1;
        }
        }
    }
for (i=0; i<10; i++){
        printf("%d\n",vetor[i]);
}
    return 0;
}
